# Cloudflare Workers (static assets) / Git integration
# Dashboard Deploy command: npx wrangler deploy
name = "torrent-parser"
compatibility_date = "2026-03-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
